-- Sprint 4 no incorpora funcionalidades: se retira el registro de búsquedas sin resultado.
-- La tabla llegó a producción vacía, así que no hay datos de usuario que conservar.
drop table if exists public.peticiones_catalogo;
